<h2>変更</h2>
<form action="/memos/<%= @id %>/edit" method="post">
  タイトル:<br>
  <input type="text" name="title" value="<%= settings.md.get_memo(@id).title %>"><br>
  本文:<br>
  <textarea name="content" cols="60" rows="5"><%= settings.md.get_memo(@id).content %></textarea><br>
  <input type="submit" value="変更">
</form>
<a href="/">ホーム</a>
